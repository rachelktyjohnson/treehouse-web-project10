extends layout

block content
    h1 Warning, this can't be undone!
    h2= "Delete '"+book.title+"'?"
    form(method="post")
        input(type="submit" class='button delete' value="Yes, delete book")
    br
    a(class='button back', href='/') < Back
